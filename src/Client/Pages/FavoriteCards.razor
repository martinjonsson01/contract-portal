@using Domain.Contracts
@inject HttpClient _http

<div id="favorite-contracts">
    <h3>Favorite Contracts</h3>
    @if (_favoriteContracts == null)
    {
        <p>
            <em>Loading...</em>
        </p>
    }
    else
    {
        <div id="favorite-cards-container" class=".d-flex">
            @if (_favoriteContracts.Length > 0)
            {
                @foreach (var contract in _favoriteContracts)
                {
                        <ContractCard Contract=@contract></ContractCard>
                }
            }
            else
            {
                <p>
                    <em>There are no favorite marked contracts</em>
                </p>
            }

        </div>
    }
</div>

@code {
    private Contract[]? _favoriteContracts;

    /// <inheritdoc />
    protected override async Task OnInitializedAsync()
    {
        _favoriteContracts = await _http.GetFromJsonAsync<Contract[]>("/api/v1/Contracts/Favorites");
    }


}
